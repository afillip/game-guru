import { FunctionalComponent } from '../stencil.core';
import { ConsumerRenderer, PropList } from '../declarations';
export declare const createProviderConsumer: <T extends {
    [key: string]: any;
}>(defaultState: T, consumerRender: ConsumerRenderer<T>) => {
    Provider: FunctionalComponent<{
        state: T;
    }>;
    Consumer: FunctionalComponent<{}>;
    injectProps: (Cstr: any, fieldList: PropList<T>) => void;
};
